language: node_js

node_js:
  - 10

cache: yarn

jobs:
  include:
    - stage: eslint
      script:
        - yarn lint
    - stage: release
      script:
        - yarn build
      deploy:
        provider: script
        skip_cleanup: true
        script: yarn semantic-release
        on:
          branch: master